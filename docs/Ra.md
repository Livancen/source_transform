## 定稿：信息架构与规则

### 一级菜单（5 项）

```
图片 | 视频 | 比例裁剪 | 自定义裁剪 | 拼接
```

| Tab            | 列表过滤                | 处理范围                   | 交互形态      |
| -------------- | ----------------------- | -------------------------- | ------------- |
| **图片**       | 仅图片                  | 仅列表内图片（可批量）     | 选项 + 开始   |
| **视频**       | 仅视频                  | 仅列表内视频（可批量）     | 选项 + 开始   |
| **比例裁剪**   | 图+视频（或可切换过滤） | 批量，当前列表文件         | 多比例 + 开始 |
| **自定义裁剪** | 选 1 个文件（图或视频） | **单文件**，绝对像素       | 内嵌预览框选  |
| **拼接**       | 选 2 个（双图或双视频） | **固定 2 路**，不混图+视频 | 内嵌槽位布局  |

原则：**图/视频批量互不混合**；裁剪/拼接为独立工作台；比例裁剪单独入口。

---

### 各 Tab 功能

#### 图片（能做的全开）

压缩（质量 / 可选降分辨率）、单独改分辨率、转格式、旋转  
（区域裁剪不在此；比例裁剪走独立 Tab）

#### 视频（能做的全开，不含区域裁剪/拼接）

压缩、改分辨率、降码率、Profile/Level、H.265→H.264、转格式、旋转、静音、帧率  
（区域裁剪已移除；比例/拼接走独立 Tab）

#### 比例裁剪

- 图 + 视频，可批量
- 多比例输出
- **命名走自身规范**（不受设置页通用命名影响）

#### 自定义裁剪

- 每次 1 个文件（图或视频）
- 绝对像素坐标
- 下方内嵌预览（视频取帧 / 图片原图），不弹窗、不批量

#### 拼接

- 双视频 **或** 双图片（不混排、不 3 路）
- 上下 / 左右
- 内嵌工作台
- 后期：自由画布拖拽布局（本次可预留，不做）

---

### 输出与命名（设置页）

| 规则     | 说明                                              |
| -------- | ------------------------------------------------- |
| 输出目录 | 设置中配置（沿用现有 input/output）               |
| 通用命名 | 可选拼接：①原名 ②时间戳 ③标准时间 ④自定义文本     |
| 生效范围 | **除比例裁剪外** 全部模式                         |
| 比例裁剪 | 继续专用命名：`原名-比例(比值)-(范围)-(日期).ext` |

---

### 必须遵守的约束（实现时）

1. Tab 切换 → 列表只显示当前类型（比例裁剪例外：图+视频）
2. 开始/导出只处理**当前列表（或当前选中）**
3. 图片 Tab 与视频 Tab **不能**一次混合处理
4. 自定义裁剪：单文件 + 像素框
5. 拼接：2 路同类；画布版后期再做
6. 视频批量 **无** 区域裁剪入口

---

### 主按钮文案建议

| Tab        | 按钮                    |
| ---------- | ----------------------- |
| 图片       | `开始处理图片 (N)`      |
| 视频       | `开始处理视频 (N)`      |
| 比例裁剪   | `按比例裁剪 (N×比例数)` |
| 自定义裁剪 | `导出裁剪`              |
| 拼接       | `导出拼接`              |

---

### 实现时注意（技术向）

| 项               | 说明                                                   |
| ---------------- | ------------------------------------------------------ |
| 图片比例裁剪     | 需新增逻辑（现仅 `crop_videos_by_ratios`）             |
| 自定义裁剪支持图 | 预览改为图片直读；裁切 ImageMagick/FFmpeg 分支         |
| 拼接支持双图     | `merge_videos` 扩展或 `merge_media`                    |
| 通用命名         | 设置页 + 后端统一拼输出名（比例裁剪绕开）              |
| 区域裁剪         | 从 `ProcessOptions` / 视频选项条移除，迁入自定义裁剪页 |

---

### 当前状态

产品规则已齐，**可以开做**。

下一步任选其一即可：

1. 出各 Tab 字段级线框 / 组件拆分
2. 直接改前端骨架（5 Tab + 列表过滤 + 内嵌裁剪/拼接）
3. 先做设置页「输出命名」+ 后端命名统一

你说从哪一步开始即可。
